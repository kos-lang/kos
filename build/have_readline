#!/bin/sh

# SPDX-License-Identifier: MIT
# Copyright (c) 2014-2020 Chris Dragan

set -e

DIRNAME=$(dirname "$0")

SRC="$DIRNAME/../core/kos_getline.c"

CC="$1"

OBJ=$(mktemp)

if "$CC" -DCONFIG_READLINE -c -o "$OBJ" "$SRC" >/dev/null 2>&1; then
    echo "true"
fi

rm -f "$OBJ"
